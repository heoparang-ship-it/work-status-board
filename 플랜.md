# 업무 현황판 플랜

## 한 줄 컨셉

업무 현황판은 "직장 멀티버스"를 탑뷰 2D 도트 사무실로 표현한 저사양 업무 시뮬레이션 보드다. 사용자는 로그인한 뒤 자신의 캐릭터에게 명령을 내리고, 출근/퇴근/외근/업무 상태가 사무실 화면과 로그 패널에 실시간으로 남는다.

## 목표

- 실제 업무 상태를 가볍고 직관적으로 확인할 수 있는 현황판을 만든다.
- 게임처럼 보이지만, 핵심은 출근/퇴근/업무/외근 상태 기록이다.
- 낮은 사양에서도 부드럽게 돌아가는 탑뷰 2D 도트 UI를 우선한다.
- 복잡한 협업툴이 아니라, 사무실 안에 누가 있고 무엇을 하는지 한눈에 보이게 한다.

## 핵심 화면

### 1. 로그인 화면

- 사용자는 아이디와 패스워드로 접속한다.
- 신규 사용자는 아이디 및 패스워드를 생성할 수 있다.
- 계정 생성 시 기본 캐릭터가 자동 생성된다.
- 로그인 성공 시 해당 사용자의 캐릭터로 사무실에 입장한다.
- 로그인 이벤트는 "출근"으로 처리되며 로그에 기록된다.

### 2. 탑뷰 사무실 화면

- 흰색 계열의 깔끔한 사무실을 탑뷰 2D 도트 스타일로 표현한다.
- 책상, 의자, 회의실, 입구, 달력, 공용 공간을 배치한다.
- 모든 캐릭터는 작은 도트 아바타로 표시한다.
- 캐릭터의 현재 상태를 위치와 간단한 아이콘으로 구분한다.
- 출근/퇴근 로그는 사무실 화면 가장 상단에 탑뷰 오버레이 형태로 표시한다.

### 3. 오른쪽 상태바

- 모든 행동 로그는 오른쪽 상태바에 시간순으로 표시한다.
- 최근 로그가 가장 잘 보이도록 상단 또는 고정 영역에 노출한다.
- 로그 예시는 다음과 같다.
  - `09:01 김민수 출근`
  - `09:15 김민수 업무 시작`
  - `10:30 김민수 외근 선택`
  - `10:31 김민수 사무실 입구에서 대기`
  - `18:05 김민수 퇴근`

### 4. 달력 화면

- 사무실 안의 달력 오브젝트를 클릭하면 월간 출근 로그가 열린다.
- 월별로 사용자별 출근/퇴근 기록을 확인할 수 있다.
- 날짜별로 출근 시간, 퇴근 시간, 외근 여부를 표시한다.
- 초기 MVP에서는 월간 캘린더와 사용자별 필터만 제공한다.

## 필수 기능

### 계정 생성

- 사용자는 아이디와 패스워드를 생성한다.
- 동일 아이디는 중복 생성할 수 없다.
- 패스워드는 저장 전 암호화한다.
- 계정 생성 후 기본 캐릭터 이름은 아이디와 동일하게 둔다.

### 로그인 및 출근 처리

- 로그인 성공 시 사용자는 자동으로 출근 상태가 된다.
- 캐릭터는 사무실 내부의 기본 자리 또는 입구에서 시작한다.
- 오른쪽 상태바에 출근 로그가 표시된다.
- 상단 출근/퇴근 로그 영역에도 해당 기록이 반영된다.

### 퇴근 처리

- 사용자는 퇴근 버튼 또는 캐릭터 메뉴에서 퇴근을 선택할 수 있다.
- 퇴근 시 캐릭터는 사무실에서 사라지거나 입구 밖으로 이동한다.
- 퇴근 로그는 오른쪽 상태바와 상단 로그에 모두 표시된다.

### 캐릭터 명령

- 로그인 사용자는 자신의 캐릭터에만 명령을 내릴 수 있다.
- 캐릭터 클릭 시 명령 메뉴가 열린다.
- 1차 명령은 다음 두 가지를 우선 제공한다.
  - 외근
  - 업무

### 외근

- 사용자가 자신의 캐릭터를 클릭하고 외근을 선택한다.
- 캐릭터는 사무실 입구로 이동한다.
- 입구에 도착하면 대기 상태가 된다.
- 상태바에는 외근 선택 및 입구 대기 로그가 남는다.
- MVP에서는 실제 외부 지도 이동 없이, 입구 대기 상태까지만 구현한다.

### 업무

- 사용자가 자신의 캐릭터를 클릭하고 업무를 선택한다.
- 캐릭터는 자기 자리 또는 지정된 업무 공간으로 이동한다.
- 상태는 업무 중으로 변경된다.
- 상태바에 업무 시작 로그가 표시된다.

## 주요 오브젝트

### 사용자 캐릭터

- 로그인 유저를 대변하는 도트 캐릭터다.
- 클릭 시 명령 메뉴를 제공한다.
- 상태에 따라 간단한 말풍선 또는 아이콘을 표시한다.

### 사무실 입구

- 외근 선택 시 캐릭터가 이동하는 목적지다.
- 외근 대기 상태를 표현하는 핵심 위치다.

### 달력

- 월간 출근 로그를 여는 인터랙션 오브젝트다.
- 클릭 가능한 오브젝트임을 살짝 강조한다.

### 오른쪽 상태바

- 전체 이벤트 로그를 담당한다.
- 사용자의 행동, 출근, 퇴근, 업무 시작, 외근 대기를 기록한다.

### 상단 출근/퇴근 로그

- 사무실 화면 상단에 표시되는 핵심 출퇴근 현황 영역이다.
- 전체 로그보다 짧고 명확하게 오늘의 출근/퇴근만 보여준다.

## 화면 구성 초안

```text
┌──────────────────────────────────────────────────────────────┬────────────────────┐
│ 오늘 출근/퇴근 로그                                           │ 전체 상태 로그       │
├──────────────────────────────────────────────────────────────┤                    │
│                                                              │ 09:01 김민수 출근    │
│        [달력]                                                │ 09:15 업무 시작      │
│                                                              │ 10:30 외근 선택      │
│   책상    책상    회의실                                      │ 10:31 입구 대기      │
│                                                              │                    │
│        캐릭터                                                │                    │
│                                                              │                    │
│                              [입구]                          │                    │
└──────────────────────────────────────────────────────────────┴────────────────────┘
```

## MVP 개발 범위

### 1단계

- 계정 생성
- 로그인
- 로그인 시 출근 로그 생성
- 탑뷰 사무실 기본 화면
- 내 캐릭터 표시
- 오른쪽 상태 로그 표시

### 2단계

- 캐릭터 클릭 메뉴
- 업무 선택
- 외근 선택
- 외근 선택 시 입구 이동 및 대기
- 상단 출근/퇴근 로그 표시

### 3단계

- 달력 오브젝트 클릭
- 월간 출근 로그 모달
- 사용자별 출근/퇴근 이력 확인
- 퇴근 처리

## 데이터 구조 초안

### User

- `id`
- `username`
- `passwordHash`
- `characterName`
- `createdAt`

### Character

- `id`
- `userId`
- `x`
- `y`
- `status`
- `direction`

### WorkLog

- `id`
- `userId`
- `type`
- `message`
- `createdAt`

로그 타입 예시:

- `clock_in`
- `clock_out`
- `work_start`
- `outside_selected`
- `outside_waiting`

### AttendanceLog

- `id`
- `userId`
- `date`
- `clockInAt`
- `clockOutAt`
- `outsideCount`

## 상태 규칙

- 로그인 성공은 출근으로 간주한다.
- 같은 날 이미 출근한 사용자가 다시 로그인하면 중복 출근 로그를 만들지 않고 재접속 로그만 남긴다.
- 퇴근 후 다시 로그인하면 정책을 정해야 한다.
  - MVP 권장안: 같은 날 퇴근 후 재로그인은 재출근이 아니라 재접속으로 처리한다.
- 외근 선택 후 캐릭터가 입구에 도착해야 외근 대기 로그를 남긴다.
- 업무 선택 시 기존 상태가 외근 대기여도 업무 상태로 되돌릴 수 있다.

## UX 원칙

- 첫 화면은 설명 페이지가 아니라 바로 로그인 화면이다.
- 로그인 후에는 곧바로 사무실이 보여야 한다.
- 사용자가 조작할 수 있는 대상은 자신의 캐릭터임이 명확해야 한다.
- 로그는 항상 보이고, 화면 전환 없이 현재 상태를 이해할 수 있어야 한다.
- 도트 그래픽은 귀엽게 만들되 업무 도구로서 방해되지 않게 절제한다.

## 추후 확장 아이디어

- 회의실 예약 상태 표시
- 자리 비움, 점심, 집중 업무 모드
- 팀별 구역 분리
- 캐릭터 커스터마이징
- 관리자용 월간 근태 리포트
- 슬랙 또는 메신저 상태 연동
- 실제 사무실 좌석 배치도 업로드

## 우선순위 결론

가장 먼저 만들어야 할 것은 예쁜 도트 사무실 전체가 아니라, 로그인한 사용자가 자신의 캐릭터로 출근하고, 캐릭터에게 업무 또는 외근 명령을 내리며, 그 결과가 오른쪽 로그와 상단 출퇴근 영역에 남는 흐름이다. 이 흐름이 완성되면 "직장 멀티버스"의 기본 재미와 업무 현황판으로서의 실용성이 동시에 살아난다.
